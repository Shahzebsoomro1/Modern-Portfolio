# UI-only Portfolio Preview

This folder contains a standalone UI/UX preview of the portfolio as static HTML/CSS. It uses only placeholder SVGs and no images or documents from the original project.

How to preview:

1. Open `ui-only/index.html` in a browser (double-click or use `Live Server`).

That's it — the pages are static and meant for visual review and handoff.
