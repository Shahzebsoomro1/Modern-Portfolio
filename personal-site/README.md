# Personal-site UI Preview

This folder contains your personal UI-only portfolio preview.

How to preview:

1. Open `personal-site/index.html` in a browser (double-click or use `Live Server`).

Update images:

- Place your profile image at `personal-site/assets/profile_user.jfif` to show it in the header and About page.
